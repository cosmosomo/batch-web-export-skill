---
name: batch-web-export
description: 批量网页导出/下载工作流。适用于需要登录的网站上重复执行"进入列表项→点击导出→选择格式→确认下载"的批量操作，典型场景包括：学术数据库（Scopus、Web of Science、CNKI、PubMed、IEEE）批量导出文献/引用/施引文献、电商平台（淘宝、京东）批量导出订单/商品、后台系统（CRM、OA、财务）批量导出报表/数据、社交媒体批量导出内容。包含单篇验证、批量执行、失败重试、集合交集核对、按业务键重命名的完整闭环。当用户要求"批量导出""批量下载""逐一点开然后导出""把这些都导出来"且目标是网页操作时使用。
---

**版本**: v1.1.0 | **测试平台**: 豆包桌面端

> **生态边界**：本 Skill 的浏览器自动化执行层依赖豆包 `seed_browser_use` API（`bu.find()` / `bu.click()` 等）。方法论、`verify_and_rename.py`、Scopus JS 代码通用，其他 Agent 可参考但需改写 `bu.xxx()` 调用。

# Batch Web Export

在需要登录的网页上批量执行重复导出操作的标准化工作流。核心原则：**先验证再放大、用稳定方式定位元素、用可控批量大小控制风险、用核对闭环消灭沉默失败**。

## 工作流总览

```
阶段1 前置准备 → 阶段2 单篇验证 → 阶段3 批量执行 → 阶段4 失败重试+核对交付
```

每个阶段都有明确的完成标准，未达标不进入下一阶段。

---

## 阶段1：前置准备（必做）

### 1.1 明确任务边界
- 操作对象是谁？（哪个网站、哪个列表、多少项）
- 每项执行什么操作？（导出什么、什么格式）
- 产出什么？（多少个文件、什么命名、是否打包）

### 1.2 确认登录态和权限
- 目标网站是否已登录？权限是否足够执行导出？
- 如果未登录，调用 `interaction.request_action(type="browserControl")` 让用户接管登录。

### 1.3 识别列表结构
- 目标列表是无限滚动还是分页？
- 能否设置每页显示数量？（优先设为最大，减少翻页）
- 能否通过 URL 参数直接定位到每个操作页？（避免逐一点击进入）

### 1.4 构造 URL 模板（关键优化）
如果能从列表项的链接中提取唯一标识（如 ID、EID、recordId），直接构造操作页 URL：
```
https://example.com/export?id={item_id}
```
直接导航比"返回列表→点击下一项"快 3-5 倍，且避免列表页状态丢失。

### 1.5 估算总量和批量大小
- 单篇耗时 × 批数 ≤ 单调用超时上限 × 60%
- 经验值：网页导出单篇约 10-15 秒，120 秒超时 → 每批 6 篇
- 总批数 = 总项数 ÷ 每批数，向上取整

**完成标准**：URL模板可用、总量明确、每批大小确定。

---

## 阶段2：单篇验证（必做，约10分钟）

**永远不要直接批量跑。** 先用 1 项跑通完整流程，确认每一步可靠。

### 2.1 跑通完整流程
对第 1 项执行完整操作，记录每一步的：
- 定位方式（ref / JS选择器 / 坐标）
- 等待时间
- 成功信号

### 2.2 元素定位优先级（从高到低）
| 方式 | 可靠性 | 适用场景 | 注意事项 |
|------|--------|----------|----------|
| `bu.find("文本")` + ref点击 | 最高 | 按钮、链接、菜单 | ref是文档作用域标识，**不受视觉遮挡影响** |
| JS查询DOM点击 | 中 | 动态菜单选项、无稳定文本的元素 | 用 `[role="menuitem"]` 等属性定位 |
| 快照解析ref | 低 | 兜底 | **快照有长度限制，长页面会被截断**，可能找不到底部元素 |
| 坐标点击 | 最低 | Canvas/无DOM元素 | 分辨率变化会失效 |

**关键经验**：对话框确认按钮通常是 `bu.find("确认/Export/导出")` 返回的**最后一个**ref。不要用可见性筛选（弹窗可能挡住按钮导致 `offsetParent === null`）。

### 2.3 验证产出物
- 文件是否存在？
- 大小是否 > 0？
- 抽样打开确认格式和内容正确？
- **导出范围是否完整**：CSV行数（含表头）是否等于预期数据条数+1？（如施引文献565条，CSV应有566行）
- **筛选器是否误勾选**：确认左侧筛选面板没有意外勾选（如"All open access"）

### 2.4 测试边界情况
- 0 条结果的项（导出是否会报错？是否需要跳过？）
- 1 条结果的项（对话框打开是否较慢？）
- **高引用/多结果的项**（施引>200条时，是否选了"Documents 1 - N"而非"All documents on this page"？CSV行数是否完整？）
- 导出按钮灰色不可点的情况（是否需要先全选文献？）

### 2.5 定义失败信号
明确什么算失败：超时？空文件？跳转错误？弹窗报错？

**完成标准**：1篇完整跑通、产出验证正确、边界情况有处理方案、失败信号明确。

---

## 阶段3：批量执行

### 3.0 推荐：使用批量执行引擎模板

**不要从零写批量循环。** 使用 `scripts/batch_export_template.py` 执行引擎模板：

1. 复制模板全部代码到 `computer_use_tool(plane="bu")` 的 code 参数中
2. 修改 `CONFIG` 字典（URL模板、批量大小、等待时间、输出目录、命名模板）
3. 填入 `ITEMS` 列表（从列表页抓取的预期项，每项含ID和业务字段）
4. 实现/修改3个自定义函数（`navigate_to_item` / `open_export_and_select_format` / `confirm_export_and_wait`）
5. 执行即可

引擎自动处理：分批控制、失败收集、下载后立即重命名、失败项统一重试（调参）、进度输出、最终统计。Scopus 的3个函数已预置实现，通常不需要修改。

### 3.1 单篇执行模板（如不使用引擎模板，参考以下伪代码）
```
对每个 item_id：
  1. bu.navigate(url_template.format(item_id))
  2. bu.wait_for_load() + sleep(等待时间)
  3. 打开导出菜单：bu.click(bu.find("导出/Export").first)
  4. 选择格式：JS点击对应菜单项
  5. sleep(格式选择后等待时间)
  6. 确认导出：bu.click(bu.find("确认/Export").last)  ← 最后一个ref
  7. record = bu.wait_for_download(超时时间)
  8. 验证：record.state == "completed" 且 文件大小 > 0
  9. 重命名：os.rename(record.path, "{业务键}.{ext}")
```

### 3.2 批量控制规则
- **每批不超过阶段1确定的数量**（通常6篇）
- 每篇执行后立即验证（文件存在 + 大小>0 + 命名正确）
- **失败项记录到列表，不重试，继续下一篇**（当场重试会打乱节奏导致整批超时）
- 每批完成后输出进度：成功X / 失败Y / 剩余Z

### 3.3 下载文件处理
- 下载后**立即重命名**为业务键（如 `{排名}_{ID}.csv`），不要用系统默认名
- 序号用零填充（`001` 而非 `1`），确保文件管理器排序正确
- 保存到独立的输出目录，不要和其他文件混在一起

**完成标准**：所有项执行完毕，成功/失败列表完整。

---

## 阶段4：失败重试 + 核对交付

### 4.1 统一重试失败项
- 收集阶段3所有失败项的ID
- **针对性调参**后重试（不要用相同参数重复）：
  - 下载超时 → 增加 `wait_for_download` 超时（25s→35s）
  - 对话框未出现 → 增加格式选择后等待时间（2.2s→3.5s）
  - 页面加载慢 → 增加导航后等待时间
- 重试后仍失败的项，单独记录并告知用户

### 4.2 全量核对（核心闭环）
**用唯一标识做集合交集，不要肉眼比对：**
```python
downloaded_ids = set(本地文件名中的ID)
expected_ids = set(目标列表抓取的ID)
assert downloaded_ids == expected_ids  # 数量必须完全匹配
```
输出：
- 下载数 vs 预期数
- 交集数（完全匹配数）
- 缺失项列表（预期有但下载没有）
- 多余项列表（下载有但预期没有）

### 4.3 生成核对清单表格
做一个CSV/Excel表格，每行包含：
- **预期值列**：排名、ID、关键属性（如引用数、标题）
- **实际值列**：是否已下载、重命名后文件名
- 用户打开表格筛选"是否已下载=否"即可看到缺失项

### 4.4 生成全局索引（可选但推荐）
如果批量导出的是更大集合的子集（如"总266篇中的第101-200名"），生成完整集合的排行/索引表，让用户知道这批数据在全局中的位置。

### 4.5 打包交付
- 压缩包命名包含：来源 + 范围 + 内容 + 格式（如 `Scopus_引用排行101-200_施引文献CSV.zip`）
- 交付物三层结构：原始文件 + 核对清单 + 全局索引

**完成标准**：失败项处理完毕、集合交集100%匹配、核对清单生成、压缩包交付。

---

## 关键经验速查

| 问题 | 解决方案 |
|------|----------|
| 快照被截断找不到底部按钮 | 用 `bu.find()` 代替快照解析 |
| 弹窗挡住按钮，JS按可见性筛选跳过了 | 用ref点击，不要用 `offsetParent` 筛选 |
| 批量执行中途超时 | 每批≤超时×60%，失败项不重试继续下一篇 |
| 低引用/少结果的项导出慢 | 重试时增加等待时间，不要首轮就加大 |
| 文件名无意义无法追溯 | 下载后立即用业务键重命名，序号零填充 |
| 不知道导出是否完整 | 用唯一标识集合交集核对，生成核对清单 |
| 0条结果的项导出报错 | 前置过滤跳过，或捕获异常继续 |

---

## 平台特定参考

- **Scopus 学术数据库**：见 [references/scopus-specific.md](references/scopus-specific.md)
  - 排序select的value映射、分页设置、引文链接URL模板、Export菜单结构
- **高阶技巧与最佳实践**：见 [references/advanced-techniques.md](references/advanced-techniques.md)
  - 网络请求捕获、资源阻塞提速、高效选择器、可见性精确判断、Tab管理、速率限制、安全防护
- **常见陷阱与排查**：见 [references/common-pitfalls.md](references/common-pitfalls.md)
  - 各类失败的诊断方法和解决方案

## 可复用脚本

- **批量执行引擎**：`scripts/batch_export_template.py`
  - 完整的批量导出执行框架，包含分批控制、失败收集、立即重命名、统一重试、进度统计
  - 使用时只需修改 CONFIG、填入 ITEMS、实现3个网站特定函数
  - Scopus 的3个函数已预置实现
- **核对与重命名**：`scripts/verify_and_rename.py`
  - 输入：预期ID列表、下载目录、命名模板
  - 输出：核对清单CSV、按业务键重命名的文件、缺失/多余报告
  - 命名模板缺字段时自动用ID兜底，避免全部失败
